name: deliver

on:
  schedule:
    # UTC 22:30 = JST 翌朝 07:30。
    # analyze.yml は UTC 21:30 開始 / timeout 45min (最長 UTC 22:15 終了) なので、
    # analyze 完了から deliver 開始まで 15min のマージンを確保する。
    # 通常 analyze は 25-30min 程度で終わるため実マージンはより大きい。
    - cron: '30 22 * * *'
  workflow_dispatch:

concurrency:
  group: deliver
  cancel-in-progress: false

permissions:
  contents: read
  issues: write     # 失敗時 Issue 起票

jobs:
  deliver:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: '20'
          cache: 'npm'

      - run: npm ci

      - run: npm run typecheck

      - name: Run deliver
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
          RESEND_FROM_EMAIL: ${{ secrets.RESEND_FROM_EMAIL }}
          RECIPIENT_EMAIL: ${{ secrets.RECIPIENT_EMAIL }}
        run: npm run deliver

      - name: Open issue on failure
        # cancelled() を含めるのは timeout-minutes 到達による中断を検知するため。
        # GitHub Actions の failure() は cancel をカバーしない仕様。
        if: failure() || cancelled()
        uses: actions/github-script@v9
        with:
          script: |
            const { owner, repo } = context.repo;
            const runUrl = `https://github.com/${owner}/${repo}/actions/runs/${context.runId}`;
            const title = `[deliver] workflow failed (run ${context.runId})`;
            // 直近 24 時間に同種 Issue があればコメント追加で済ませる (Issue 乱立防止)
            const since = new Date(Date.now() - 24 * 60 * 60 * 1000).toISOString();
            const existing = await github.rest.issues.listForRepo({
              owner,
              repo,
              state: 'open',
              labels: 'deliver-failure',
              since,
              per_page: 5,
            });
            const body = `workflow が失敗しました。\n\nrun: ${runUrl}\ntime: ${new Date().toISOString()}`;
            if (existing.data.length > 0) {
              const issue = existing.data[0];
              await github.rest.issues.createComment({
                owner,
                repo,
                issue_number: issue.number,
                body,
              });
            } else {
              await github.rest.issues.create({
                owner,
                repo,
                title,
                body,
                labels: ['deliver-failure'],
              });
            }
